export const add = (a: number, b: number): number => {
  return a + b;
};

export const subtract = (a: number, b: number): string => {
  console.log(`Subtraction of values ${a}&${b} are : ${a} - ${b}`);
  return `Subtraction of the values ${a}&${b} are : ${a} - ${b}`;
};
